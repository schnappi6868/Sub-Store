{
  "name": "sub-store",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm run build:worker",
    "build:worker": "esbuild src/worker.js --bundle --format=esm --outfile=dist/worker.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@sub-store/common": "^1.0.0",
    "@sub-store/core": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "wrangler": "^3.0.0"
  }
}
